# TASKS.md

本文件是项目任务管理中心，用 Markdown Checklist 记录当前任务、待办事项、Bug 和已完成事项。

原则：

- 新任务写到“当前任务”或“待办事项”。
- 已确认的问题写到“Bug 列表”。
- 完成后移动到“已完成事项”并勾选。
- 长期业务知识不要写在这里，应写入 `MEMORY.md`。

## 优先级说明

- `P0`：生产不可用、数据严重错误、安全风险。
- `P1`：核心业务功能异常，影响主要使用流程。
- `P2`：体验问题、性能问题、非阻塞功能缺口。
- `P3`：优化、重构、文档完善。

## 当前任务

- [x] `P1` 建立 AI Context Engineering 基础文档：`AGENTS.md`、`MEMORY.md`、`TASKS.md`。
- [x] `P1` 创建全局 `$ai-context-engineering` skill，用于以后给任意项目初始化或审计上下文文档。
- [x] `P1` 第一阶段模块化重构：建立模块命名、旁路总览页面、权限兼容和路由拆分。
- [ ] `P1` 部署后验证基础看板和货柜流转看板的订单号、国家、到岸、签收、在途数据是否正确。
- [ ] `P2` 验证货柜流转看板筛选、搜索、节点选中动画和缓存时间跳动是否符合业务使用习惯。

## 待办事项

- [ ] `P1` 为新闻抓取补充更完整的诊断日志，区分请求失败、无结果、被过滤、限额跳过。
- [ ] `P1` 完善智能解析写入的关联字段选择体验，统一 AI 候选和人工复核流程。
- [ ] `P1` 为智能解析新增“新增记录 / 修改记录 / 纯手工录入”的稳定操作路径。
- [ ] `P2` 为穿透弹窗增加更多维度：订单、柜号、温度记录、物流记录、分柜记录。
- [ ] `P2` 将更多看板统计口径开放到后台配置，减少硬编码。
- [ ] `P2` 补充项目级 smoke test 脚本，快速验证核心 API 和页面脚本语法。
- [ ] `P3` 整理 README，使其与当前认证、缓存、新闻、AI 解析和流转看板保持一致。

## Bug 列表

- [ ] `P1` 生产环境部署后需要确认基础看板“到岸 / 签收 / 在途”不再异常归零。
- [ ] `P1` 生产环境部署后需要确认分柜明细中的订单引用字段不再显示记录 ID。
- [ ] `P1` 生产环境部署后需要确认基础看板越南订单数、物流在途批次和关口滞留已切到分柜状态口径。
- [ ] `P2` 检查不同屏幕尺寸下 `index-flow.html` 是否一屏展示且不重叠。
- [ ] `P2` 检查秘塔新闻启用状态、调用次数和积分消耗是否符合配置预期。

## 已完成事项

- [x] `P1` 建立项目上下文三件套，确保新对话可按 `AGENTS.md` → `MEMORY.md` → `TASKS.md` 快速恢复项目理解。
- [x] `P1` 创建全局 `$ai-context-engineering` skill，位置为 `/Users/sweetlemon/.codex/skills/ai-context-engineering`。
- [x] `P1` 增加企业微信智能表内存快照缓存，供看板和穿透读取。
- [x] `P1` 增加货柜流转看板 `/index-flow.html`。
- [x] `P1` 为货柜流转看板增加筛选、搜索、两排流转图、缓存时间跳动和节点选中动效。
- [x] `P1` 修复货柜流转看板中订单引用字段显示记录 ID 的问题。
- [x] `P1` 让基础看板聚合优先使用后台配置字段 ID，降低字段改名导致统计归零的风险。
- [x] `P1` 修正基础看板国家订单数，避免双品类订单在鲜果/冻果拆行后重复计数。
- [x] `P1` 修正物流监控在途批次与关口滞留口径，改为结合分柜当前状态判断。
- [x] `P1` 修正货柜流转看板筛选后总柜数/未发货统计，改为继续保留订单主表总量。
- [x] `P1` 修复模块化权限后 `index.html` 温度甘特图因仍检查旧 `gantt` 权限而无法加载的问题。
- [x] `P2` 增强旁路运营总览 `/app-overview.html`，补充订单、物流、温度、新闻多模块概览和多图表展示。
- [x] `P2` 将旁路运营总览调整为深蓝黑紧凑一屏版，并补充核心指标和模块数字口径说明。
- [x] `P2` 将旁路运营总览升级为老板经营驾驶舱，新增老板指标聚合接口并减少重复柜量展示。
- [x] `P2` 为老板经营驾驶舱增加国家/工厂/柜号筛选、7 天对比、温度缩略甘特和数据穿透弹窗。
- [x] `P2` 优化老板经营驾驶舱穿透体验：状态中文化、风险单柜穿透、温度明细穿透、瓶颈比例说明、结构甜甜圈和 KPI 快照环比基础。
- [x] `P1` 增加后台统计设置入口，可配置交付率相关口径。
- [x] `P1` 增加 AI 智能解析录入方向：自然语言或截图解析后，人工确认再写入智能表。
- [x] `P2` 增加新闻源后台配置入口，包括秘塔相关配置。

## 示例任务写法

- [ ] `P1` 修复某核心接口异常：说明接口、复现方式、期望结果。
- [ ] `P2` 优化某页面体验：说明页面、目标用户、验收标准。
- [x] `P3` 完成某文档更新：说明更新范围和关联文件。
